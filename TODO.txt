Track proggress and write down next steps:
- GENERALIZACIJA, predavanje OI
- Import data to model✅
- MFCC/MFE hyperparameters
- Split data into Train, Validate, Test -> join them all together, shuffle, and split into 70/10/20✅
- NN hyperparameters: most important may be LEARNING_RATE, then BACH_SIZE, EPOCH(when val_acc drops), LAYERS(dense, conv, dropout...)...
